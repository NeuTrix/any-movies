import React, { Component } from 'react'
import PropTypes from 'prop-types'

const Comment = (props) => {
  const (comment)
  return (
    <div>
      {comment.author}
      {comment.title}
      {comment.body}
    </div>
    );
}
 
export default Comment;